#include <iostream>
using namespace std;

int main()
{
    float a, b, c;

    cout << "enter the value of a : " << endl;
    cin >> a;

    cout << "enter the value of b : " << endl;
    cin >> b;

    cout << "enter the value of c : " << endl;
    cin >> c;

    cout << "the sum of the three no is : " << a + b + c << endl;
    cout << "the average of the three no is : " << (a + b + c) / 3 << endl;

    return 0;
}
